"""Configuration file containing psstgresql connection info.This is just an
example. The real file (postgresql_config) will not be pushed because it
contains sensitive data."""

PG_URL = "dbname=database_name host=localhost port=5400"
